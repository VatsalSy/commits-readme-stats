name: Test Action
on: 
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
